+on:
+  schedule:
+    - cron: "0 */6 * * *"   # every 6 hours (UTC)
+  workflow_dispatch:        # allow manual runs
+  push:
+    paths:
+      - .github/scripts/fetch-cfbd.mjs
+      - .github/workflows/fetch-cfbd.yml

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20 }
      - run: node .github/scripts/fetch-cfbd.mjs
        env:
          CFBD_KEY: ${{ secrets.CFBD_API_KEY }}

      - name: Commit & push data
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
+         git pull --rebase
          git add data/*.json
-         git commit -m "chore(data): update via CFBD" || echo "No changes"
-         git push
+         if ! git diff --cached --quiet; then
+           git commit -m "chore(data): update via CFBD"
+           git push
+         else
+           echo "No changes"
+         fi
